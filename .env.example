# Frontend public key (safe to expose)
VITE_ONESIGNAL_APP_ID=your-onesignal-app-id

# Do NOT place REST API keys in frontend env files.
# Keep OneSignal REST key only in Supabase secrets:
# supabase secrets set ONESIGNAL_REST_API_KEY=...
# supabase secrets set ONESIGNAL_APP_ID=...
